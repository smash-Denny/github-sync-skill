# v1.0.0

## 核心变化

- 初始版本
- 支持书籍拆解类（book-class）自动识别
- 支持独立工具类（tool-class）上传
- 自动生成 meta SKILL.md 入口
- 自动归档匹配模式的旧 repo

## 文件结构

- `github-sync-skill.py` - 主脚本
- 书籍类仓库结构：`references/` + `skills/` + `SKILL.md(meta入口)` + `_meta.json`
- 工具类仓库结构：根目录直接 `SKILL.md` + `test-prompts.json`

## 适用范围

- Denny-cangjie 蒸馏出的所有 skill 仓库
